# Frontend (React + Vite)

## Prerequisite
Install Node.js 20+.

## Start
```bash
npm install
npm run dev
```

The app runs on `http://127.0.0.1:5173` and calls Django API at `http://127.0.0.1:8000`.
